int main()
{
    clock_t t1, t2;
    t1 = clock();
    int x,y,k;
        for (x=0;x<100;x++){
            for(y=0;y<256;y++){
                for(k=0;k<10;k++) printf("test");
            }
        }
       t2 = clock();
       float diff = (((float)t2 - (float)t1) / 1000000.0F ) * 1000;
       printf("%f",diff);
    return 0;
}
